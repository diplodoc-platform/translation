# xliff

rules and hooks for custom renderer to render xliff document with `translation-units` with text segments out of markdown file.

- rules: render markdown tokens as xml
- hooks: generate xml document
